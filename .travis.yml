language: php

php:
  - 5.5
  # - 5.6

cache:
  directories:
    - $HOME/.composer/cache

sudo: false

before_install:
  - composer selfupdate

install:
    - composer update --prefer-dist --no-interaction

script:
    - sh ./bin/scripts/travis_phpunit.sh
    - sh ./bin/scripts/travis_coverfish.sh
    - sh ./bin/scripts/travis_coverage.sh
    # - sh ./bin/scripts/scrutinizer_ccup.sh

notifications:
  email:
    - patrick.paechnatz@gmail.com
    - dunkelfrosch.net@gmail.com